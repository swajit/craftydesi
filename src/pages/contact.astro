---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact" description="Request a quote for a custom woodworking project.">
	<section class="contact-hero">
		<div class="container text-center">
			<h1>Request a Quote</h1>
			<p>Have a project in mind? Tell me about it below and I'll get back to you with an estimate.</p>
		</div>
	</section>

	<section class="contact-section">
		<div class="container">
			<div class="contact-grid">
				<form action="https://formspree.io/f/xoqykqyb" method="POST" class="contact-form">
					<div class="form-group">
						<label for="name">Full Name</label>
						<input type="text" id="name" name="name" required placeholder="John Doe" />
					</div>
					<div class="form-group">
						<label for="email">Email Address</label>
						<input type="email" id="email" name="email" required placeholder="john@example.com" />
					</div>
					<div class="form-row">
						<div class="form-group">
							<label for="type">Project Type</label>
							<select id="type" name="project_type">
								<option value="furniture">Furniture</option>
								<option value="decor">Home Decor</option>
								<option value="kitchen">Kitchenware</option>
								<option value="other">Other</option>
							</select>
						</div>
						<div class="form-group">
							<label for="budget">Budget Range</label>
							<input type="text" id="budget" name="budget" placeholder="$500 - $1000" />
						</div>
					</div>
					<div class="form-group">
						<label for="message">Message / Project Details</label>
						<textarea id="message" name="message" rows="6" required placeholder="Tell me what you're looking for..."></textarea>
					</div>
					<button type="submit" class="btn btn-primary btn-large">Send Request</button>
				</form>

				<div class="contact-info">
					<div class="info-card">
						<h3>Next Steps</h3>
						<ol>
							<li><strong>Review:</strong> I'll review your request within 2-3 business days.</li>
							<li><strong>Consultation:</strong> We'll discuss details, wood choice, and timeline.</li>
							<li><strong>Deposit:</strong> A 50% deposit secures your spot in the build queue.</li>
						</ol>
					</div>
					<div class="info-card">
						<h3>Location</h3>
						<p>CraftyDesi Woodworking<br />Small Shop, Big Dreams<br />Your City, State</p>
					</div>
				</div>
			</div>
		</div>
	</section>

	<style>
		.contact-hero {
			padding: 6rem 0 3rem;
			background: var(--color-surface);
			border-bottom: 1px solid var(--color-border);
		}
		.text-center {
			text-align: center;
		}
		.contact-hero h1 {
			font-size: 3rem;
			margin-bottom: 1rem;
		}
		.contact-hero p {
			font-size: 1.25rem;
			color: var(--color-muted);
			max-width: 600px;
			margin: 0 auto;
		}

		.contact-grid {
			display: grid;
			grid-template-columns: 1.2fr 0.8fr;
			gap: 4rem;
			margin-top: 4rem;
		}

		.contact-form {
			display: flex;
			flex-direction: column;
			gap: 1.5rem;
			background: var(--color-surface);
			padding: 2.5rem;
			border-radius: 12px;
			border: 1px solid var(--color-border);
		}
		.form-group {
			display: flex;
			flex-direction: column;
			gap: 0.5rem;
		}
		.form-row {
			display: grid;
			grid-template-columns: 1fr 1fr;
			gap: 1.5rem;
		}
		label {
			font-weight: 500;
			color: var(--color-primary-dark);
		}
		input, select, textarea {
			padding: 0.75rem 1rem;
			border: 1px solid var(--color-border);
			border-radius: 4px;
			font-family: inherit;
			font-size: 1rem;
			background: var(--color-bg);
		}
		input:focus, select:focus, textarea:focus {
			outline: none;
			border-color: var(--color-primary);
			box-shadow: 0 0 0 2px rgba(139, 94, 60, 0.1);
		}

		.contact-info {
			display: flex;
			flex-direction: column;
			gap: 2rem;
		}
		.info-card {
			padding: 2rem;
			border-radius: 8px;
			background: var(--color-surface);
			border: 1px solid var(--color-border);
		}
		.info-card h3 {
			margin-bottom: 1rem;
			color: var(--color-primary-dark);
		}
		.info-card ol {
			padding-left: 1.25rem;
			display: flex;
			flex-direction: column;
			gap: 0.75rem;
		}
		.info-card p {
			color: var(--color-muted);
		}

		@media (max-width: 768px) {
			.contact-grid {
				grid-template-columns: 1fr;
			}
			.form-row {
				grid-template-columns: 1fr;
			}
		}
	</style>
</Layout>
